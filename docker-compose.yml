version: '2'

services:
  wordpress:
    build: .
    container_name: "colormeshop_wp_plugin_wordpress"
    ports:
      - 8080:80
    env_file: wp.env
    volumes:
      - .:/var/www/html/wp-content/plugins/colormeshop-wp-plugin

  mysql:
    image: mariadb
    env_file: wp.env
